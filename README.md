# blog-retro
# YA METE ALGO YAEL
parte q le toca a yael 

#  cd apps/api-gateway
pnpm init
pnpm add express jsonwebtoken bcryptjs cors dotenv pg
pnpm add -D typescript ts-node nodemon @types/express @types/node @types/jsonwebtoken @types/bcryptjs @types/cors

# Inicializar TypeScript
npx tsc --init 

-ile
